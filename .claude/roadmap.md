# Roadmap — AI B2B Lead Generation & Outreach System

**Companion to:** `architecture.md` (system design/architecture lives there — this file tracks progress, not design).

The product: Apollo-sourced leads → AI research → AI-written cold email → 3/7/14-day
follow-up cadence → AI reply handling with human escalation for price asks and unknown
facts. Multi-client, deliverability-hardened, with an operator console and a read-mostly
client view.

---

## Phase Status

| Phase | Goal | Status |
|---|---|---|
| P0 — Foundations | Next.js + Supabase + RLS, mailbox OAuth (Gmail/Outlook), QStash, `events` audit log | DONE |
| P1 — Discovery | Apollo search + bulk-match (email reveal + verification in one call) → company-grouped cases → `/crm` | DONE |
| P2 — Research/Write/Send/Follow-up | Per-case research dossier → AI-written first email → rotated-mailbox send → 3/7/14-day follow-up sequencer → `/inbox` human-approval | DONE |
| P3 — Reply Handling | Inbound mailbox polling (Gmail History API / Outlook delta) → intent classification → price/not-interested/question routing → knowledge-gap escalation | DONE |
| P4 — Deliverability + Observability | Warmup ramp, bounce-rate health + auto-pause, DSN/OOO detection, suppression enforcement, kill switches, `/analytics` dashboard | DONE |

Apollo's bulk-match replaced the original separate Brightdata+Gemini lead-gen agent and
Emailable-only verification — one deterministic, mostly-LLM-free pipeline. Emailable was
later reintroduced as a *second-opinion* deliverability guard layered on top (see log).

---

## Backlog / Not Started

- Dedicated `EmailFinder` provider or a full secondary verifier, if Apollo+Emailable yield is ever insufficient.
- Upstash Workflow for durable multi-step agent runs (only if the current QStash-fanout approach hits a wall).
- A/B testing of subject lines / openers.
- Client-facing notifications (email/Slack) beyond in-app.
- More reply intents: reschedule, referral to a colleague.
- Self-serve client onboarding + billing — explicitly out of scope for MVP.

---

## Known Open Items

- **Cases show `contacted` before a human approves.** `runWriteForCase` sets the case to
  `contacted` unconditionally once the leads loop finishes, regardless of whether the
  outcome was `sent` vs `drafted`. For `human_approve` clients this makes
  `analytics_by_campaign` report cases as contacted before anyone approved the draft.
  Distinct from the earlier "claimed contacted before write ran" bug (fixed 2026-08-12) —
  this is the unconditional end-of-function write. Flagged 2026-08-13, not yet fixed.
- **No automated retention purge for `events`.** `deleteExpiredEvents` (`src/lib/db/events.ts`)
  exists but is never called by a cron/route — the table has no wired-up sweep.
- **Legal documents need counsel review.** Privacy Notice, Terms, DPA, etc. (`/legal/*`,
  rewritten 2026-07-25) reflect the system's real behavior but the legal positions
  (legitimate-interests basis, arbitration clause, liability cap, SCC elections) have not
  been reviewed by a lawyer.
- **Person-level research is disabled** via a feature flag (not deleted) as of 2026-08-11 —
  see log entry for why. Re-enabling requires revisiting the guardrails that shipped
  alongside the flag.

---

## Log

Chronological, most recent last. Entries are one-line unless a decision or an open
question needs to survive.

- **2026-07-20** — Fixed 10 code-review findings across the inbound/reply pipeline (pagination cursor safety, idempotent knowledge-answer retry, ambiguous-lead-match fail-closed, timeout race, etc).
- **2026-07-20** — Replaced the single shared research call with **one agent per subject** (company + each active lead), run concurrently, each citing its own sources into `case_knowledge`.
- **2026-07-21** — Fixed a prod-crashing bug where the server env schema (`src/lib/env.ts`) was being bundled into client code; split into `env.ts` (server) / `env-public.ts` (client-safe, literal `NEXT_PUBLIC_*` access so Turbopack inlines it).
- **2026-07-21** — First real design system: Tailwind v4 + shadcn/ui + Phosphor icons, dark-default. Built the `/cases/[id]`, `/mail`, `/knowledge` surfaces that already had DB support but no UI. Found and fixed a real RLS bypass: `/settings` was listing every client's mailboxes via the admin client with no filter.
- **2026-07-21** — `/clients` admin page + invite-link account provisioning (Supabase `generateLink`), then rebuilt into `/clients/[id]` as a header+tabs workspace (Campaigns/Analytics/Users) mirroring the case-detail UI, with pause/resume/archive/delete lifecycle actions and a 2-step confirm on the destructive ones. Pausing a client now also stops in-flight research/write/follow-up, not just new discovery.
- **2026-07-21** — Gemini calls gained an opt-in `thinkingLevel`; set to `medium` for the research agent's gather step and reply classification (judgment-heavy), left at default elsewhere.
- **2026-07-21** — Client-scoped error logging: every Gemini/Apollo/Brightdata/mailbox failure is now attributed to a client and severity-tagged (`events.severity`/`source`), surfaced later as the operator Logs tab.
- **2026-07-21** — Emailable deliverability guard shipped: a second-opinion check layered on top of Apollo's own `email_status`, fail-open on Emailable errors. Loosened 2026-08-08 (see below) after production data showed the strict policy parking too many address-unconfirmable-but-fine catch-all domains.
- **2026-07-23** — Apollo's existing bulk-match response already carries company firmographics (industry, headcount, founded year, description) — now captured into `case_knowledge` at zero extra API cost.
- **2026-07-23** — Client knowledge base: operator uploads/crawls a client's website + PDFs, chunked and embedded (pgvector), retrieved and injected into the write/follow-up/reply/knowledge-answer prompts. Similarity floor (0.5 cosine) added same day to stop weakly-related chunks leaking in.
- **2026-07-24** — Public marketing page at `/` (previously just redirected to `/crm`), copy written to sell the outcome not the machinery — no vendor names, no multi-client framing.
- **2026-07-25** — Brand rename Beacon → Shengul AI across all user-visible surfaces; typeface Geist Sans → Inter.
- **2026-07-25** — AEO/SEO pass: `robots.txt`/`sitemap.xml`/`llms.txt` were actually 302ing to `/login` (audit was scoring a login page as "present") — fixed the middleware matcher; added AI-crawler allowlist, canonical URLs, OG images, JSON-LD, FAQ schema.
- **2026-07-25** — Legal documents (`docs/legal/`) were describing an entirely different product ("Founderside AI", Stripe billing, free trial) and were never linked into the app. Rewrote all 7 documents to describe this system's real data flows and served them at `/legal/<slug>`. Needs counsel review — see Known Open Items. Contact details updated 2026-08-12 to the real operating domain (`shengulai.com`).
- **2026-07-25** — WebMCP: registered read-only tools + declarative-mutation form annotations so a browsing agent can act on the console pages without ever getting write access via a tool call directly.
- **2026-07-26** — Fixed QStash-driven pipeline workers being redirected to `/login` (405s, burning all retries) and a separate outage where invite links opened the marketing page instead of signing the user in.
- **2026-07-26** — Invite links changed from single-use to reusable within a fixed window.
- **2026-07-26 / 2026-07-28** — AI Resources: client collateral (one-pagers, case studies) the agent can attach and send; the agent now reads the actual resource file content (not just its title) when deciding what to send.
- **2026-07-28** — Client notes (internal, not sent) + client-written manual email composer added to the case thread.
- **2026-07-29 → 2026-08-13** — Mailreach warmup integration: connect a mailbox to Mailreach, gate new campaign sends until 14 days of warmup, surface warmup status in `/settings` and Reports. Iterated across several passes (initial schema/client Aug-29, SMTP-connect 404 fix Aug-04, per-client API key Aug-12, full status surfacing Aug-13, sync cadence tightened 6h → 10min Aug-13).
- **2026-07-29** — Google Tag Manager + Consent Mode v2 installed; caught that the published cookie/privacy policies claimed "no tracking, no consent banner" — rewrote those docs to match reality instead of leaving GTM undisclosed.
- **2026-08-02** — CRM integrations: HubSpot and Pipedrive two-way sync (OAuth connect, `/settings/crm`, background sync worker), shipped across 13 tasks.
- **2026-08-03** — Case Mail split into per-contact tabs; reply mode (`auto_send`/`hybrid`/`human_approve`) made client-configurable; CRM settings moved into the main `/settings` page.
- **2026-08-03** — Per-mailbox warmup caps made configurable (was a single hardcoded ramp).
- **2026-08-04** — Knowledge-base retrieval upgraded to hybrid (vector + keyword) search; added a "re-scrape all" button; mailbox connect opened up to client-role users (previously operator-only).
- **2026-08-05** — Discovery pipeline precision/cost pass (6 tasks); inbox draft gained an AI-redesign box plus manual editing; follow-up cadence (3/7/14-day) made configurable per campaign.
- **2026-08-06** — Dashboard localized to English/Turkish (18 tasks); lazy-start warmup ramp (a new mailbox doesn't start ramping until first real send); campaign settings became editable with a read-only client view.
- **2026-08-07** — Diagnosed a QStash-cron outage: all pipeline crons had silently stopped since 08-04 (fixed). Shipped per-client/per-campaign discovery scheduling (a client or campaign can now run discovery at a specific time/timezone instead of one global cron), fixed the company research agent's search tool always failing, and added a depth-first retry loop to discovery.
- **2026-08-07** — Client contact signature (phone + address) added to outbound emails.
- **2026-08-08** — Diagnosed two live production error clusters from the Logs tab (email-write JSON truncation; Bright Data search/scrape aborts) — root-caused and iterated on through 2026-08-10, see below. Upgraded the human-voice email prompt to pattern-based rules with a regeneration preview tool. Shipped a per-client formal-intro email style, applied to Uniforms Fashion (rewrote its 8 pending drafts).
- **2026-08-08** — Emailable accept-all catch-all carve-out: production data showed the `risky` bucket was 100% domain-level "can't confirm" (`accept_all: true`), not address-level bad — now activates that specific case instead of parking it. Two follow-up fixes landed within days: a stranded-lead bug where `listActiveLeadsForCase` still required `email_status = 'verified'` after the carve-out, and a client-facing label fix so a catch-all-activated lead reads "Verified" to the client (still "Risky" to the operator).
- **2026-08-09** — Closed 5 prompt gaps in the formal-intro system prompt; every email-writing agent switched to `gemini-3.6-flash`. Email styles made editable/addable via a DB-backed CRUD + UI (was hardcoded). Manual company-info field added to replace the auto-generated website-RAG "About our company" blurb (more control, less risk of a bad scrape summary reaching a prospect). `write.ts`/`redesign.ts` thinking level flipped `minimal` → `medium` — this is what caused the Cluster A truncation bug fixed the next day.
- **2026-08-10** — New `contactsPerCompany` campaign setting, fixing a bug where discovery was silently capping near 1 lead per company. Error-cluster fixes: **Cluster A** (Gemini `generateObject` truncation on write/redesign) fixed by raising the output-token ceiling 1,600 → 2,600. **Cluster B** (Bright Data search/scrape aborts) mitigated by raising every timeout in the request path — the actual fix (a concurrency cap on Bright Data calls) was explicitly deferred, later shipped 2026-08-15. Research-agent system prompts rewritten to stop hallucinated facts and to trust Apollo's own data instead of re-verifying it. Extraction step downgraded to `flash-lite` and scrape truncation tightened, both for cost. Also: campaign time-picker validation bug fixed, campaign save errors now surface which field failed.
- **2026-08-11** — Fixed a formal-intro false-footprint claim and an isolated-fact regression; tightened dossier fact-selection and negative-fact guardrails globally. **Disabled person-level research** behind a feature flag (not deleted) — see Known Open Items. Shipped the Client Home Dashboard, then a visual redesign of it same week.
- **2026-08-12** — Root-caused irrelevant email hooks for Uniforms Fashion's school-uniform campaigns and fixed it. Reports (weekly + monthly digests) shipped across 18 tasks. Pipeline latency reduced by chaining stages event-driven instead of waiting for the next cron tick. Fixed a case being claimed `contacted` before the write actually ran, and a Gemini-overload long-retry issue.
- **2026-08-13** — Landing page hero rewritten with a live-animated outcome panel. Turkish i18n + language switcher shipped for the landing page (13 tasks). Campaign mailbox picker UI added — surfaced (and backfilled) a real bug: every Uniforms Fashion campaign had `mailbox_ids: []` with no UI to ever set it, silently stranding approved drafts. In the same investigation, found the still-open unconditional-`contacted` bug (see Known Open Items). Fixed a schema bug where Emailable's `score: null` (vs. omitted) crashed validation, and added Apollo confidential-org redaction handling. Investigated a reported "Ryanair dedup not working" — confirmed correct behavior, no fix needed. Fixed discovery depth-phase freshness-cap and breadth-quota starvation bugs, plus a company-key desync and an unsafe cast found in the same review.
- **2026-08-14** — Per-campaign contact-signature override; code-review fixes for `contactsPerCompany` capacity enforcement and the live-discovery test script.
- **2026-08-14 / 2026-08-15** — Person + company social scraping: Bright Data LinkedIn/X post scraping keyed to Apollo-verified profile URLs, attributed per-lead (`case_knowledge.lead_id`) so a fact about one lead can't leak into another lead's dossier on the same case. Code review after shipping added a DB trigger enforcing the lead's `case_id`/`client_id` actually matches the knowledge row (previously silently un-enforced) and switched the FK to `on delete cascade` (was `set null`, which would have let a deleted lead's fact resurface as "company-wide").
- **2026-08-15** — Concurrency limiter added for Bright Data calls (`BRIGHTDATA_MAX_CONCURRENT = 5`, shared FIFO semaphore across search/scrape/social-discovery) — closes the Cluster B gap flagged 2026-08-10.
- **2026-08-15** — Per-campaign email templates: the client-only `email_styles`/`email_style_id` mechanism (2026-08-08/09) was renamed to `email_templates`/`email_template_id` (migration 0046) and given a `campaigns.email_template_id` override — same nullable-inherit convention as the signature override (2026-08-14). The row's content model also changed: it's now a literal reference email the operator pastes verbatim (client's own words), not abstract voice instructions — `write.ts`'s `buildSystemPrompt` wraps whatever text is there with a fixed "personalize this, don't copy it verbatim, resolve [bracketed] placeholders, pick the matching example if there's a `---`-separated multi-sample template" instruction, so the two original rows (Concise/Formal introduction, still instruction-prose) keep working unchanged. Seeded and assigned 6 of Uniforms Fashion's 8 live campaigns with the operator's real hand-written templates (Public Safety, Border & Aviation Security, Defense Prime Contractor, Transport & Ground Ops, Hospitality & Travel, Cargo & Courier — the last three each concatenate 2-3 sub-vertical samples per operator instruction). Healthcare Sector and Schools deliberately left on the client-level default — no matching sample yet, per the existing 2026-08-15 ICP-doc gap. Applied directly to prod (operator ran the migration; this session's `supabase migration list` found 0001-45 untracked in the remote history table, so a plain `db push` was avoided as unsafe). `regenerate-sample-emails.ts`/`rewrite-draft-emails.ts` updated to resolve the same campaign→client→default precedence via the new exported `resolveEmailTemplate` instead of duplicating it.
- **2026-08-15** — Client account-creation invites: TTL raised 2 hours → 3 days (`INVITE_TTL_MINUTES`, `formatInviteTtl` now day-aware); the invite link is now emailed to the client automatically (new `lib/auth/invite-email.ts` template, sent via the existing `lib/reports/mailer.ts` SMTP sender) instead of only being shown for the operator to copy. Send is best-effort — SMTP failure doesn't roll back the created login, the response/UI carry an `emailSent` flag so the operator falls back to the still-present copy button when delivery fails.
- **2026-08-16** — Fixed a client-reported "wrong warmup day" bug: `MailboxWarmupInfo.elapsedDays` (0-indexed, from `mailreach-gate.ts`) was being shown directly as "Day N" on the home banner, Reports/Analytics warmup panels, and the report-commentary LLM prompt/fallback/email template, while the settings/warmup-tab daily-cap ramp counter used a 1-indexed `dayNumber` (`elapsedDays + 1`) for the same concept — two different "Day N" meanings depending which screen a client looked at. Added `dayNumber` to `MailboxWarmupInfo` and switched every display call-site to it; `elapsedDays` stays 0-indexed internally for gating/ordering math only. `WarmupTemplateContext.closestElapsedDays` renamed to `closestDayNumber` to match.
- **2026-08-17** — Rolled the existing `RealtimeRefresher` pattern (already live on `/analytics`/`/home`) out to `/mail`, `/crm`, and `/inbox` — each now carries a pulsing "Live" badge and refreshes on relevant Supabase Realtime `postgres_changes` events (debounced `router.refresh()`), so new leads/mail/knowledge-requests appear without a manual reload. Added migration `0047` putting `knowledge_requests` into the `supabase_realtime` publication (`emails`/`leads`/`cases` were already in it since migration `0008`) and extended the shared refresher component to also listen for `knowledge_requests` INSERT/UPDATE.
- **2026-08-17** — Follow-up audit found the same gap in three more places, one of them a real bug: `client_knowledge_sources` was never added to the `supabase_realtime` publication, so `/clients/[id]`'s knowledge-scrape-status refresher had been silently dead since it shipped (a `postgres_changes` subscription on a table outside the publication never fires — Supabase drops it). Migration `0048` adds `case_knowledge`, `client_knowledge_sources`, and `client_resources` to the publication. Shipped: `KnowledgeSourcesRealtimeRefresher` (moved out of `clients/[id]` into `components/`, `clientId` now optional so `/knowledge/sources` can reuse it unfiltered across every visible client), `CaseKnowledgeRealtimeRefresher` for `/knowledge`, `ResourcesRealtimeRefresher` for `/knowledge/resources`, and a case-scoped `CaseRealtimeRefresher` (800ms debounce, shorter than the 1.5s dashboard default since this is the page open mid-conversation) for `/cases/[id]` watching its own case's emails/leads/knowledge/knowledge_requests plus the case row itself. All four pages now carry the same "Live" badge as the rest. Migrations `0047` and `0048` still need to be applied to prod (operator-run, per the established convention).
- **2026-08-17/18** — Outreach send waiting system, all 10 tasks of `docs/superpowers/plans/2026-08-17-outreach-send-waiting-system.md` (spec: same-day design doc). Root cause: `runWriteForCase` unconditionally marked a case `contacted` (+ CRM-synced "First outreach sent") at the end of its loop regardless of what actually happened — a Mailreach-gate-blocked first touch silently lost the lead forever (nothing retried it), and a `human_approve` case with only drafts falsely reported "contacted" to the client's CRM before any human approved anything. Fix: new `case_status = 'waiting'` + `cases.wait_reason` enum (`mailreach_gate`/`daily_cap`/`no_healthy_mailbox`/`awaiting_manual_approval`/`no_viable_leads`), guarded by a check constraint (migration `0049`). New `getOutreachEligibility` probe (`src/lib/mailbox/eligibility.ts`) runs before any lead work in `write.ts`, so a gated case's retry costs a DB read, not an LLM call. `runWriteForCase` now only advances to `contacted` when at least one lead actually sent; every other outcome lands on `waiting` with a specific reason. `write-fanout`'s 5-minute cron sweeps `waiting` cases whose reason is mailbox-availability-based (filtering the two non-time-based reasons before dispatch), and `write/route.ts`'s entry guard was widened to actually accept them (`AUTO_RETRY_WAIT_REASONS`) — closing what would otherwise have been a silent no-op gap between fanout and route. `approveDraft` (`/inbox`) now flips an `awaiting_manual_approval` case to `contacted` and fires the CRM sync only on real human approval, guarded so a second lead's approval on an already-`contacted` case is a no-op. `PRE_CONTACT_STATUSES` (`send-actions.ts`) and `STATUS_FILTERS` (`crm/page.tsx`) both gained `'waiting'` so a manual rescue send and the kanban filter both work correctly. `mailbox.none_healthy`'s log message now distinguishes "still warming up" from "actually broken" using the `warmupGatedCount` payload field that was already being logged but never rendered. Per-reason `CASE_WAIT_REASON` label rendering on case list/detail rows was scoped in the design (§8) as an implementation-time UI detail, not required by any task's test — not yet wired into a component; picking that up is the one remaining loose end. `pnpm typecheck && pnpm lint && pnpm test` all clean (2546 tests passing).
- **2026-08-18** — Code-review fixes on the outreach-send-waiting system: manual send (`send-actions.ts`) now enqueues the same CRM sync as draft approval when it advances a case to `contacted` (was missing entirely), and case advancement no longer aborts when `scheduleFirstFollowup` throws — the two were sharing one try/catch. `approveDraft`'s first-contact transition (`inbox/actions.ts`) switched from read-then-write to a single atomic conditional update (`claimCaseContacted` in `lib/db/cases.ts`, same pattern as `claimCollisionNotice`) — closes a race where two leads on the same case approving concurrently could both pass a stale status read and double-fire the CRM sync. `write-fanout`'s dispatchability filter was being applied after `FANOUT_LIMIT`, so a page of 200 non-retryable `waiting` cases (stuck on manual approval) could fill the whole fetch and starve a dispatchable case behind them; `listCasesByStatus` gained a keyset `after` cursor and the route now pages (bounded by `MAX_PAGES = 10`) until it collects `FANOUT_LIMIT` dispatchable cases or the queue is exhausted. All existing + new tests pass (2554 total), `tsc --noEmit` clean.
- **2026-08-18** — Second review pass on the same system: `write/route.ts`'s case-eligibility read and its `updateCaseStatus(..., 'writing')` write were two separate steps, so a retried QStash delivery racing the original could both pass the read and both claim the case, double-sending. Replaced with `claimCaseForWriting` (`lib/db/cases.ts`), an atomic `UPDATE ... WHERE status='ready' OR (status='waiting' AND wait_reason IN (...))` — same pattern as `claimCaseContacted`; the pre-existing `resumable` check stays only as a fast-path early-exit. A second finding claimed the RATE_LIMITED path in `write.ts` strands the claimed outbound email as permanently `'failed'` — verified false: `claimOutboundEmail` (`lib/db/emails.ts`) already falls through to `reclaimFailedOutboundEmail` for any `'failed'` row regardless of cause, a foundational, already-tested mechanism (present since the first commit) — skipped the code change, added a two-tick `runWriteForCase` regression test instead to lock the round trip in. 2560 tests passing, `tsc`/lint clean.
- **2026-08-18** — Diagnosed "nearly no leads researched" from live DB data (20-case batch: only 10 got a real dossier, 5 completed with zero knowledge, 5 stuck in `researching` indefinitely) and root-caused it to the 2026-08-15 Bright Data concurrency limiter: `social-scrape.ts`'s `discoverPosts()` holds its slot for its *entire* trigger-poll-download lifecycle (up to `POLL_TIMEOUT_MS` = 180s) while sharing the same 5-slot pool as `brightdata.ts`'s fast per-step search/scrape calls (seconds) — a burst of concurrent cases fills every slot with long-lived discovery jobs and starves the fast calls system-wide (also the wrong thing to unify in the first place: discovery jobs hit Bright Data's Datasets API, search/scrape hit the SERP/Unlocker `/request` zone — different products, different quotas). Fixed: split into two pools (`limitBrightdataConcurrency` for search/scrape, new `limitBrightdataSocialConcurrency`/`BRIGHTDATA_SOCIAL_MAX_CONCURRENT = 2` for discovery jobs, `brightdata-limiter.ts`). Also fixed a compounding bug in `brightdata.ts`: `withRetry` was wrapping `limitBrightdataConcurrency` instead of the other way around, so a failing call held its concurrency slot through the entire retry+backoff delay instead of releasing it for other queued callers — swapped the order so each retry attempt acquires/releases independently. New `brightdata-limiter.test.ts` + retry/concurrency-interaction test in `brightdata.test.ts`; full suite (2564 tests) and `tsc --noEmit` clean. The 5 already-stuck cases from before this fix are unaffected by it (running against the old code) — still sitting in `researching`, will auto-recover via `stuck-sweep`'s 30-min reset unless manually reset sooner.
- **2026-08-18** — Code-review fixes, three findings: (1) `send-actions.ts`'s manual first-touch send had the same read-then-write race `claimCaseContacted` was built to close (2026-08-18 earlier), just with a narrower allowlist (`PRE_CONTACT_STATUSES`) than that helper's own `!= contacted` check would allow — using `claimCaseContacted` as-is would have let a manual send incorrectly walk an already-`won`/`in_conversation`/`hot_handoff`/`lost`/`dead` case (a second lead on the same case can still have an unclaimed first-touch slot) back down to `contacted`. Added `claimCaseContactedFrom` (`lib/db/cases.ts`) — same atomic shape, parameterized to an explicit source-status allowlist — and switched `send-actions.ts` to it. (2) `write-fanout`'s `listCasesByStatus` keyset cursor paged on `created_at` alone, which silently drops any row sharing the exact boundary timestamp with the last row of a page forever — a real risk given cases are bulk-created in batches. Cursor is now composite (`created_at`, `id`), via `.or()` matching the existing `claimCaseForWriting`/`case-crm-links.ts` precedent for that filter shape. (3) Two tests in `brightdata-limiter.test.ts` (this session's own) asserted before releasing their held concurrency-pool gates — a failed assertion would've left those slots permanently stuck for every later test in the file, the exact bug already caught and fixed in `brightdata.test.ts`'s retry test earlier the same session. Wrapped in try/finally. All three verified against current code before fixing (the pagination and send-actions races were both real; the third was a self-inflicted bug from earlier the same day). Full suite (2571 tests), `tsc --noEmit`, and lint all clean.
- **2026-08-18** — Migration `0049` (previous entry) failed against prod with Postgres `55P04`: its own check constraint referenced the `'waiting'` enum value in the same transaction as the `ALTER TYPE ... ADD VALUE` that created it — the exact mistake `0040`/`0041` already established the split for. Whole migration rolled back cleanly (verified live, nothing partial). Fixed by following that same precedent: `0049` now does only the `ADD VALUE`; the `wait_reason` column/enum/constraint moved to new `0050_case_wait_reason.sql`. Both applied to prod after the fix.
- **2026-08-18** — Redid the 7 genuinely-thin-research cases from the concurrency-starvation incident (Panda Hotel, Elkhart PD, The Surrey/Corinthia, Farmington PD, Naples Airport, UPS, The Courier Company — CBP/DHS/CATRION excluded, they'd already gotten a real dossier). First attempt still came back with 0 new knowledge for all 7 despite the concurrency fix — root cause turned out to be unrelated: **the Bright Data account itself was suspended for billing** (`x-brd-err-msg: "Account is suspended..."`, surfacing through our code as the same generic "Unexpected response shape" the concurrency incident also produced, since Bright Data returns an empty 200 rather than a clean error status — worth a follow-up fix to surface `x-brd-err-msg`/`x-brd-error-code` explicitly instead of failing schema validation on an empty body). Operator provided a working replacement key; re-ran with it and 4/7 cases came back with real, substantial dossiers (7-9 facts) grounded in genuine specifics (UPS: Pullman Brown color spec + PMS 462C; Courier Company: TCC/PHSE rebrand across 4 UK depots; Panda Hotel: 1,026 rooms; Farmington PD: recent patch-design update). Surrey Corinthia needed a second research pass to find anything (then got 8 facts). Elkhart PD still has only the Apollo firmographic line after two passes — plausibly a small department with minimal public web presence, not confirmed as a remaining bug. One-off script `scripts/redo-research-and-drafts.ts` (not a generic reusable tool, case IDs hardcoded to this incident) drives `runResearchForCase` + regenerates drafts via the same claim-guarded `updateDraftContent` path `rewrite-draft-emails.ts` uses. All 7 cases' status corrected `contacted` -> `waiting/awaiting_manual_approval` (nothing was ever actually sent — the pre-migration unconditional-`contacted` bug had mislabeled them). Verified directly against the DB: 0 emails sent, every draft grounded, no duplicates. **Local `.env.local`'s `BRIGHTDATA_API_KEY` was updated to the new key — production's Vercel env var still needs the same update, not yet confirmed done.**
- **2026-08-19** — Root-caused the raw Next.js "Server Components render" crash reported after editing a client's phone number (clients/[id]'s `EditSignatureDialog`) and hitting save: the save's `router.refresh()` re-executes the whole route tree including `(app)/layout.tsx`, which fetches draft/knowledge-request counts and client-brand data on every render — but no `error.tsx` existed above it (`src/app/error.tsx` was missing entirely), so a `page.tsx`-level boundary like `clients/[id]/error.tsx` can't catch a throw from its own *layout*, only from the page below it. Any transient failure in that layout's queries fell through to Next's bare, unstyled, English, digest-only default screen instead of this app's localized panel — reproducible from any page, not specific to the phone field. Added `src/app/error.tsx` (localized via next-intl, reuses `ErrorPanel`) and `src/app/global-error.tsx` (last resort for the root layout's own failures, static English since next-intl's provider lives in the layout it would be replacing). Also fixed a real second bug found while tracing this: `ErrorPanel`'s retry button was hardcoded English ("Try again") on every existing error.tsx despite this being a client-facing, Turkish-localized app — now uses the (previously unused) `common.retry` key. Underlying transient-failure cause in `(app)/layout.tsx`'s queries not identified (no error tracking/digest access from here) — this closes the missing-boundary gap, not the root flakiness; worth adding if it recurs. 2571 tests, `tsc --noEmit`, lint all clean.
- **2026-08-19** — Root-caused a separate, unrelated "can't edit the number in a draft email" report: `/inbox`'s `DraftRow` renders its edit/approve/attachments/redesign UI for any authenticated user, but all four `inbox/actions.ts` Server Actions (`approveDraft`, `updateDraftAttachments`, `updateDraftContent`, `regenerateDraftContent`) hard-gated on `role === 'operator'` — a client-role user could open the editor, type a new number, and always get `UNAUTHORIZED` on save, silently. Confirmed with the user this restriction should open up to the owning client too (not just operators), matching the existing `sendManualEmail` (`cases/[id]/send-actions.ts`) precedent. Switched all four actions to the established RLS-scoped-read + `canManageClient` ownership-check pattern (`stopLead`/`sendManualEmail`): each now reads the email via a session-bound client first (so a cross-tenant `emailId` already resolves to `null` under RLS) then explicitly checks `canManageClient(appUser, email.client_id)` before using the admin client to write — operators keep full access, and a client can now edit/attach/redesign/approve-and-send only their own client's drafts, never another client's. `answerKnowledgeRequest` deliberately left operator-only (out of scope — a different workflow, not asked about). Test file updated to match (cross-tenant-reject + owning-client-succeeds cases added per action); 45/45 passing, `tsc`/lint clean.
- **2026-08-19** — Root-caused the flood of "Mailbox oauth persist failed" log entries (recurring every ~1min across many mailboxes) plus a related "Mail operation exceeded its deadline" send failure. `updateMailboxOauth`'s (`lib/db/mailboxes.ts`) compare-and-swap `.eq('oauth', previousOauth)` passed a raw JS object into postgrest-js's `.eq()`, which builds the filter via plain template-literal interpolation (`` `eq.${value}` ``) — a JS object stringifies to the literal text `[object Object]`, an invalid jsonb literal, so the DB update errored on *every* CAS-guarded oauth persist (i.e. every refreshed-token save, in both `reader.ts` and `sender.ts`). Reproduced directly against the real `@supabase/supabase-js` query builder (not the hand-mocked tests, which never exercised the real serialization) — confirmed the generated URL literally contained `oauth=eq.%5Bobject+Object%5D`. Because the refresh was never actually persisted, every subsequent read/send had to re-refresh the token with the provider (extra round-trip under the 10s `MAIL_DEADLINE_MS`, a plausible contributor to the adjacent timeout failures) and risked a hard auth failure with providers that rotate/invalidate refresh tokens on use. Fix: `JSON.stringify(previousOauth)` before the `.eq()` call — jsonb equality in Postgres is semantic, so key order doesn't need to match storage. New regression test in `mailboxes.test.ts` exercises the real query builder (fake `fetch`, asserts the URL param) so this can't silently regress back to the mocked-only coverage that missed it. Full suite (2578 tests), `tsc --noEmit` clean.
- **2026-08-19** — Manually resent the 12 outbound emails stranded `failed` by the incident above (all client Uniforms Fashion, first-touch step 0). Investigation found they split into two groups: 7 cases already `contacted` (another lead on the same case had sent, silently stranding this one forever — a separate, still-open gap: `runWriteForCase` never revisits a `contacted` case) and 4 cases (Panda Hotel, Elkhart PD, The Surrey, Farmington PD) that are 4 of the 7 cases from the 2026-08-18 redo-research incident, sitting in `waiting/awaiting_manual_approval` — their `failed` row held stale pre-redo content because `redo-research-and-drafts.ts`'s `updateDraftContent` call only touches `status='draft'` rows and silently skipped these (already `failed`, not `draft`). Client explicitly approved sending all 12 exactly as originally drafted, no regeneration. One-off `scripts/resend-failed-outreach.ts` (not committed — deleted after use) reclaimed each `failed` slot and resent through the real `sendViaMailbox` path (no bypass flags), so health/rotation/suppression/daily-cap/mailreach-gate all still applied. Result: all 12 correctly rejected with `RATE_LIMITED` — the campaign's one mailbox had already used its full day-1 warmup cap (12/12) before the run — nothing sent, nothing corrupted, cap respected. Still pending: re-run once the cap resets.
- **2026-08-19** — While investigating the above, found and fixed a second, more consequential gap in the same family: `approveDraft` (`inbox/actions.ts`, the human-approval send path in `/inbox`) caught a `sendViaMailbox` failure by marking the email `failed` and rethrowing — never touching the case's `wait_reason`. A `RATE_LIMITED` failure (daily cap / mailreach gate / no healthy mailbox) at the exact moment of approval therefore left the case in whatever state it already was, which is not in `AUTO_RETRY_WAIT_REASONS` for the common case (`awaiting_manual_approval` is explicitly excluded, since a human is supposed to re-approve — but the draft is already gone, having flipped to `failed` and dropped out of `listDraftEmailsForClient`'s `status='draft'` filter). Net effect: a client clicking Approve during a cap/warmup window loses the email permanently with zero visibility, worse than the incident above (that at least left a queryable `failed` row an operator could find). Fixed by mirroring `runWriteForCase`'s own `RATE_LIMITED` handling: on that specific error code, re-run `getOutreachEligibility` and call `updateCaseWaiting` with the real reason, so the case lands back in the auto-retry set and `write-fanout`'s 5-minute sweep picks it up once mailbox availability clears — matching the eligible-but-still-rate-limited race the same way `write.ts` does (falls back to `'daily_cap'`). Non-`RATE_LIMITED` failures are untouched (still just `markEmailFailed` + rethrow, unchanged behavior for a genuine send/provider error). 6 new tests in `inbox/actions.test.ts`; full suite (2583 tests), `tsc --noEmit`, lint all clean.
- **2026-08-19** — Fixed the third and last gap in the same family, this time in the fully-automated path: `runWriteForCase` (`write.ts`) processes every active lead on a case in one loop, then decides the case's fate once, after the loop — `if (sent > 0) { contacted }` takes precedence unconditionally, even when another lead in that *same* run hit `RATE_LIMITED` (e.g. lead A's send exhausts the campaign's daily cap mid-loop, lead B then fails). Once the case is `contacted`, nothing ever revisits it — confirmed directly in prod: case `1c0fc550` ("The Sofa Delivery Company") has two leads, both `failed`, case `contacted`. This is exactly what produced 7 of the 12 stranded emails from the incident above, and will recur for any multi-lead case going forward, not just this one incident. Root-caused before fixing: case status is a single-value enum, so a case can't be `contacted` *and* carry a retry `wait_reason` at the same time, and the natural alternative (letting `runWriteForCase` re-run against an already-`contacted` case) would mean loosening `claimCaseForWriting`'s claim predicate — the same delicate, already-many-times-hardened race-condition surface (`claimCaseContacted`, `claimCaseContactedFrom`) — for what should be a narrow, low-volume edge case. Chose a decoupled fix instead: a new bounded sweep (`sweepFailedFirstTouch`, `lib/pipeline/resend-failed.ts`) that queries `emails` directly for `status='failed', direction='outbound', sequence_step=0` (new `listFailedFirstTouchEmails` in `lib/db/emails.ts`) — independent of case status entirely — and retries each through the real `sendViaMailbox` path, reusing the exact stored subject/body (never regenerated, matching the client's own stated preference from the manual resend above: approved content must not change). Skips a lead that's no longer `active`, a case already closed out (`won`/`lost`/`dead` — a fresh cold email must not resurrect a case a human or the CRM explicitly ended), or an inactive campaign. Races safely against the normal write-fanout path via the same atomic `.eq('status','failed')` reclaim guard `claimOutboundEmail` already had — whichever side wins, the other just sees a taken slot and skips; no double-send. New `/api/pipeline/resend-failed` route (QStash-verified, mirrors `write-fanout`'s shape but does the sweep directly — no per-item fanout, since each retry is one `sendViaMailbox` call, not an LLM call) and `scripts/schedule-resend-failed-cron.ts` (default hourly, not write-fanout's 5-minute — the dominant real cause is a daily cap that only clears at UTC midnight). Mutation-tested one assertion to confirm the test suite actually catches a regression before trusting it. 20 new tests (emails.test.ts, resend-failed.test.ts, route.test.ts); full suite (2603 tests), `tsc --noEmit`, lint all clean. **Not yet registered against prod** — the code is inert until `scripts/schedule-resend-failed-cron.ts` is actually run; deliberately left for explicit confirmation before a new autonomous recurring send-capable job goes live unsupervised.
- **2026-08-19** — Cap-blocked send waiting system (spec + plan: `docs/superpowers/specs/2026-08-19-cap-blocked-send-waiting-design.md`, `docs/superpowers/plans/2026-08-19-cap-blocked-send-waiting.md`), closing the last gap in the `RATE_LIMITED`-handling family above: a `RATE_LIMITED` send on content that already exists (LLM-generated, or client-approved) was still being treated identically to a genuine delivery failure — `markEmailFailed`, discarded, and (for `human_approve`) silently regenerated via a fresh LLM call on the next auto-retry, which violates the standing constraint that client-approved content must never be altered post-approval. New `email_status = 'waiting'` (migration `0051`) and `case_wait_reason = 'awaiting_resend'` (migration `0052`, deliberately excluded from `AUTO_RETRY_WAIT_REASONS`) mark "content committed, blocked by a transient mailbox condition, will auto-retry verbatim" — distinct from `'failed'`, now reserved for genuine non-retryable errors. Every `RATE_LIMITED` catch across `write.ts` (`processLead`/`runWriteForCase`, plus a per-lead `listWaitingLeadIds` pre-filter so a lead already parked skips the LLM call entirely), `approveDraft` (`inbox/actions.ts` — now returns `{status:'sent'|'waiting'}` instead of throwing, with a matching `/inbox` toast + i18n keys), and `followup.ts` (`runFollowupStep` — previously the worst gap: no reschedule at all, a cap-blocked follow-up just silently stopped the sequence forever) now parks the row `'waiting'` instead of `'failed'`. `resend-failed.ts`'s drain sweep widened from first-touch-only to every `'waiting'` outbound row (`listWaitingOutboundEmails`/`claimWaitingOutboundEmail` replace the old `'failed'`-scoped functions); a follow-up-step branch (new) resends as-is, recomputes `inReplyTo`/threading via `listThreadEmails`, and resumes cadence bookkeeping (`getSequenceByLeadId`, new) — advance-or-exhaust exactly as `runFollowupStep` does on a normal send. Cron cadence tightened from hourly to write-fanout's own 5-minute tick (`scripts/schedule-resend-failed-cron.ts` default `*/5 * * * *`) so already-committed content gets priority over the same day's brand-new work close to a UTC-midnight cap reset. Executed inline (no worktree/branch, no subagent orchestration, no incremental commits, per direct instruction) directly against `master`; full suite (2618 tests), `tsc --noEmit`, `eslint src scripts` all clean. **Two things still open, both deliberately not run by this session:** (1) migrations `0051`/`0052` written but **not applied** — this project's Supabase CLI is linked directly to prod with an untracked local-vs-remote migration history (confirmed via `supabase migration list`), the same condition that made `db push` unsafe on 2026-08-15, so applying them needs the same manual operator step 0049/0050 used; (2) the cron cadence change is committed to the script but **not re-registered** against the live QStash schedule — needs an explicit run of `scripts/schedule-resend-failed-cron.ts` against prod, same sign-off bar as any other prod cron change.
- **2026-08-19** — Code-review fixes on the cap-blocked send waiting system (previous entry). `approveDraft` (`inbox/actions.ts`) had two real gaps: (1) the `RATE_LIMITED` branch swallowed a `markEmailWaiting` failure and still returned `{status:'waiting'}` — the row was actually left stuck `'queued'` (invisible to both the drain sweep and the generation paths) while the caller was told it succeeded; now retries the write once and, if that still fails, lets the error propagate instead of reporting a false success (`updateCaseWaiting` stays separate best-effort handling, unaffected). (2) losing the atomic `claimDraftForSend` race unconditionally returned `{status:'sent'}` — true only when the concurrent approval that won had actually finished; a lost claim can equally mean `'queued'` (still mid-send), `'waiting'`, or `'failed'`. `approveDraft`'s return type widened to `'sent'|'waiting'|'failed'|'in_progress'`, re-reading the row on a lost claim instead of assuming; `/inbox`'s `DraftRow` and both locale files updated to match (new `toastApprovalInProgress` key). Also fixed a stale comment on `/api/pipeline/resend-failed`'s route still describing the pre-widening first-touch-only/`'failed'`-only sweep. Two other findings from the same review pass were checked against current code and are not being changed: `claimWaitingOutboundEmail`'s lack of a claim-lease/stale-reclaim mechanism is a real theoretical gap but shared by every claim-based transition in this table (`claimDraftForSend` included) and would need a schema migration to fix properly — flagging rather than doing that unprompted; `updateMailboxOauth`'s silent no-op when the CAS filter matches no rows is the documented, intended behavior (a concurrent refresh already won) and is locked in by an existing regression test asserting exactly that. 56/56 `actions.test.ts` plus the full touched-file suite (179 tests) pass, `tsc --noEmit` and lint clean.
- **2026-08-19** — Second code-review pass on the same waiting-system family, five findings. Three real bugs in `resend-failed.ts`'s `resendOne`: (1) every permanent-skip branch (malformed row, inactive/emailless/deleted lead, closed/deleted case, archived/deleted campaign, an inbound reply arriving mid-wait, an already-stopped sequence) just returned `'skipped'` forever with the row still `'waiting'` — re-fetched and re-skipped by every future sweep tick indefinitely, since nothing ever moved it to a terminal state. Now calls `markEmailFailed` on each of those (a merely-`'paused'` campaign is deliberately left alone — an operator's pause is expected to resume). (2) the follow-up thread-lookup only excluded the row being resent itself, not any other same-lead row that was never actually delivered (`provider_message_id: null` — e.g. a manual send still mid-flight via `send-actions.ts`'s two-step insert-then-send) — if that row happened to be newest by `created_at`, it would win `.at(-1)` and produce a broken `In-Reply-To` reference instead of the real prior message; now filtered out. (3) the worst one: the whole post-send bookkeeping block (`markEmailSent`, follow-up scheduling, case/sequence advancement) shared the same `try/catch` as `sendViaMailbox` itself, contradicting the function's own comment claiming otherwise — a bookkeeping failure *after* a real, successful send (e.g. `enqueueCrmSync` throwing) got treated identically to a delivery failure, corrupting an already-sent email's status to `'failed'` in the DB. `write.ts` and `followup.ts` (both already correct on this exact point) were the reference for the fix: `sendViaMailbox` now sits in its own try/catch, everything after runs unconditionally outside it. Separately, both `write.ts`'s `processLead` and `followup.ts`'s `runFollowupStep` had the same latent gap `inbox/actions.ts` had earlier today: a `markEmailWaiting` failure in the `RATE_LIMITED` catch left the row stuck `'queued'` — worse here than in `actions.ts`, since `'queued'` is a dead end for *every* retry path (`claimOutboundEmail`'s upsert no-ops against the occupied slot, its own fallback only reclaims `'failed'`, and the drain sweep only polls `'waiting'`). New shared-shape `parkAsWaiting` helper (one per file, matching each file's existing local-helper convention) retries the write once, then falls back to `markEmailFailed` (still reclaimable) and rethrows rather than silently reporting a routine `'waiting'`/`'skipped'` outcome that would desync the case-level `wait_reason` bookkeeping from the row's real status. A fifth finding (`/inbox` `DraftRow` footer showing "Sent ✓" for a `'waiting'`/`'failed'`/`'in_progress'` approval outcome, not just true `'sent'`) was also confirmed valid and fixed: `isSent` boolean replaced with a proper `ApproveOutcome | null` state, four distinct footer chips (icon/color/label per outcome, `--status-waiting` reused for the waiting chip), two new i18n keys per locale. 16 new/updated tests across `resend-failed.test.ts`, `followup.test.ts`, `write.test.ts`; full suite (2634 tests), `tsc --noEmit`, lint all clean.
- **2026-08-20** — Incident: cases stuck at `writing` reported by the user. Root-caused (not guessed — reproduced live) via `systematic-debugging`: migrations `0051`/`0052` were exactly the "still open" item flagged in the 2026-08-19 entry above (written, deliberately not applied to prod). Confirmed directly against the live DB: any query touching `email_status = 'waiting'` or `case_wait_reason = 'awaiting_resend'` failed with Postgres `22P02 invalid input value for enum` — the enum values genuinely didn't exist in prod even though the app code (already deployed) assumed they did. Every `RATE_LIMITED`-parked write hit this, threw an uncaught `DB_ERROR` out of `listWaitingLeadIds`, and left the case at `writing` forever from that call's perspective — but stuck-sweep's 30-min/`~15-min-cron` cadence kept reclaiming and re-crashing it, producing the ~45-minute repeating "Write job crashed" cycle the user was seeing. Live event log: 220 `pipeline.write.route_failed` events since 2026-08-18 20:06 UTC, 204 of them this exact enum error, across 28 distinct cases (17 sitting at `writing` at time of investigation, all one client). User applied both `alter type ... add value` statements to prod directly (Supabase SQL editor); re-probed live and confirmed both enum values now accepted. The 17 stranded cases were left to self-heal via the existing stuck-sweep cron rather than force-resumed manually, since forcing them immediately would have triggered live sends — no code change needed, this was a deploy-order gap (code shipped ahead of its own migration), not a logic bug.
- **2026-08-20** — Same `writing`-stuck incident, two more findings after the migration fix landed. (1) Once the enum bug was gone, stuck-sweep's own next requeue of the same 17 cases fired 17 concurrent Gemini calls at once; 11 hit "high demand" (503-equivalent) after the AI SDK's internal retry exhausted (3 attempts) — and `isModelOverloadedError` (`llm/client.ts`) never caught it, because the AI SDK's default `createRetryError` (`node_modules/.../@ai-sdk/provider-utils`) discards the original `APICallError` once its own retries are exhausted and throws a bare `Error(message)` instead — so `toLlmAppError`'s `APICallError.isInstance(cause)` check always fails post-exhaustion, `statusCode`/`isRetryable` are never populated, and a genuinely transient overload falls through to the same generic dead-end path as any other failure instead of `handleModelOverload`'s dedicated long-retry. Fixed in `llm/client.ts`: `toLlmAppError` now falls back to matching known Gemini/Vertex overload phrasing ("high demand", "model is overloaded", "resource_exhausted", "rate limit exceeded") in the plain `Error`'s message text and sets `isRetryable: true` when no `APICallError` instance is available to read it from structurally — only used when the structured signal is absent, so a call that does give us a real `APICallError` is unaffected. 2 new tests (fallback fires on the real observed message, doesn't false-positive on an unrelated exhausted-retry message). (2) Separately, those 11 cases then missed two consecutive stuck-sweep ticks in a row by single-digit seconds — root-caused as a structural resonance, not bad luck: `STUCK_THRESHOLD_MINUTES` (30) was an exact multiple of the cron's own 15-minute dispatch interval, so a batch-requeue's claim delay (QStash+route latency, worse for bigger batches — spread up to 30s across the 17-case batch) reliably outran the recheck tick's own few-seconds of jitter, because both landed at the same phase of the 15-minute clock grid every cycle. Fixed by changing the threshold to 22 minutes (`stuck-sweep/route.ts`) — off the 15-minute grid, so the first tick that can cross it now carries minutes of real margin instead of seconds. Both fixes: full suite (2652 tests), `tsc --noEmit`, lint all clean. **Not yet committed or deployed** — sitting in the working tree pending confirmation, per direct instruction not to commit without being asked.
- **2026-08-20** — Root-caused and fixed the Uniforms Fashion client's "our sent mail isn't showing up in our own inbox" report. Their mailboxes are generic SMTP/IMAP (Mailreach-connected, `provider: 'smtp'`); `smtp-send.ts` only ever submitted over SMTP, which is a one-way relay to the recipient — unlike Gmail (`SENT` label applied automatically) and Graph (`saveToSentItems: true`), plain SMTP never touches the account's own IMAP store, so nothing was ever filed into that mailbox's Sent folder. (Mailreach warmup mail *did* show up because Mailreach independently uses the same IMAP credentials, from the `imap_auth` connect, to file its own warmup sends into Sent — confirming the same gap existed on our send path.) Fix: new `smtp-sent-copy.ts` (`appendSentCopy`) rebuilds the exact sent message via nodemailer's `MailComposer`, reusing the real `providerMessageId` so the filed copy threads correctly, resolves the account's Sent folder via the RFC 6154 `\Sent` special-use flag (`imapflow`'s `client.list()`) with a common-name fallback (`Sent`/`Sent Items`/etc.), and `client.append()`s it with `\Seen`. Wired into `smtp-provider.ts` as a best-effort step after a successful send — failure there is logged (`mailbox.sent_copy_failed`) and never fails the send itself, never retries/duplicates. 22 new tests (`smtp-sent-copy.test.ts`, `smtp-provider.test.ts`); full suite (2650 tests), `tsc --noEmit`, lint all clean.
